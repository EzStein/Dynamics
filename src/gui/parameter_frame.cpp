#include "gui/parameter_frame.h"

namespace dynsolver {
namespace gui {

parameter_frame::parameter_frame(app& app, int id) :
    parameter_frame_base(NULL), appl(app), id(id) {
}
} // namespace gui
} // namespace dynsolver
